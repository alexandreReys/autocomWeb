import { store as auth } from '@/modules/auth'
import { store as watchlist } from '@/pages/watchlist'
import { store as showSerie } from '@/pages/show-serie'
import { store as marcas } from '@/pages/marcas'
import { store as tipos } from '@/pages/tipos'
import { store as produtos } from '@/pages/produtos'

export default {
  auth,
  watchlist,
  showSerie,
  marcas,
  tipos,
  produtos
}
